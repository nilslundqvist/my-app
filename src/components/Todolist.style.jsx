import styled from "styled-components";

export const ListStyle = styled.label`
    background:transparent;
    color: rgb(14, 97, 93);
    font-weight: 700;
    -webkit-font-smoothing: antialiased;
    display: block;
    line-height: 1.5;
    margin-bottom: 5%;
`